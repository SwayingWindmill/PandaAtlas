# Issue #193 Structured Contribution Map-Close Evidence

Issue #193 closes the #174 structured contribution and review-to-archive map. It does not add a new business write path. It turns the deferred evidence from #186, #187, #188, #189, and #192 into one release-gate-certified package.

## Evidence package

The authoritative machine-readable matrix is:

- `data/release-evidence/issue-193-structured-contribution-review-to-archive.json`

The matrix must stay valid under:

- `node scripts/release/validate-structured-contribution-evidence.mjs`
- `npm run release:map-close`

## Closure boundary

#174 may close only when the final PR records:

1. a passing Development gate;
2. a passing map-close Release Gate;
3. Linux and Windows clean-checkout evidence or explicit CI artifact links;
4. browser/mobile/WCAG evidence for contributor, reviewer, Feed, Inbox, and admin surfaces;
5. Supabase reset and Foundation preflight evidence through the final migration;
6. database and attachment restore evidence;
7. immutable `.release-gate/map-close-manifest.json` and `.release-gate/map-close-manifest.sha256` artifact hashes;
8. rollback-switch evidence for Community Intake, Review Moderation, Community Curation Bridge, Notification, and Archive publication features.

## Required #193 categories

The validator enforces these categories exactly:

- `draft-validation-submit-revisions`
- `private-upload-attachment-security`
- `reviewer-workflow`
- `curation-release-projection`
- `notifications-email`
- `browser-mobile-wcag`
- `operations-privacy-recovery`

Each category must contain covered scenarios, evidence references, and at least one gate command or immutable artifact location. A scenario with `status != "covered"` blocks final closure.

## Blocker status

The prerequisite map slices are closed before final certification:

- #186 closed via PR #212.
- #187 closed.
- #188 closed.
- #189 closed.
- #192 closed via PR #224.

## Deferred-to-final artifact rule

Some evidence is necessarily generated by final certification rather than by static repository state. These are represented as artifact locations in the matrix and are sealed by `scripts/release/map-close-evidence.mjs` after `release:map-close` produces `default.json` and `map-close.json`.

## Non-goals

Issue #193 does not reinterpret community review as Archive approval, does not grant publication capabilities, and does not bypass #175 self-publication or sensitive-content rules. It verifies the integrated contribution path and records the evidence needed to close #174.
