name: Sync Issue 198 With Mainline

on:
  push:
    branches:
      - feat/issue-198-privacy-operations
    paths:
      - .github/workflows/sync-issue-198.yml

permissions:
  contents: write

jobs:
  sync-mainline:
    runs-on: ubuntu-latest
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v7.0.0
        with:
          ref: feat/issue-198-privacy-operations
          fetch-depth: 0

      - name: Merge current master without discarding Privacy work
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin master
          git merge --no-commit -X ours origin/master
          rm .github/workflows/sync-issue-198.yml
          git add -A
          git commit -m "merge: sync privacy operations with moderation mainline"
          git push origin HEAD:feat/issue-198-privacy-operations
